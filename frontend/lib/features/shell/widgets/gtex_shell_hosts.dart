import 'package:flutter/material.dart';

class GtexToastHost extends StatelessWidget {
  const GtexToastHost({super.key, required this.child});

  final Widget child;

  @override
  Widget build(BuildContext context) => child;
}

class GtexModalHost extends StatelessWidget {
  const GtexModalHost({super.key, required this.child});

  final Widget child;

  @override
  Widget build(BuildContext context) => child;
}

class GtexDrawerHost extends StatelessWidget {
  const GtexDrawerHost({super.key, required this.child});

  final Widget child;

  @override
  Widget build(BuildContext context) => child;
}
