# GTEX Active-Shell Action Integrity Matrix

| Action | Originating screen | Backend mutation endpoint | Persisted? | Role/context required | Current status | Fix applied |
| --- | --- | --- | --- | --- | --- | --- |
| Home CTA navigation | `frontend/lib/features/home/home_screen.dart` | none | n/a | none | LIVE | Verified all active CTAs route to the correct active-shell destinations. |
| Claim daily challenge | `frontend/lib/features/tasks/tasks_screen.dart` | `POST /daily-challenges/{challengeKey}/claim` | yes | authenticated session | LIVE | Verified shipped tasks screen uses the real claim endpoint and invalidates live task state afterward. |
| Buy player shares | `frontend/lib/features/transfer_market/transfer_market_screen.dart` | `POST /players/{player_id}/shares/buy` | yes | authenticated session | LIVE | Verified live mutation path; UI does not claim success unless the backend call returns. |
| Add transfer listing to watchlist | `frontend/lib/features/transfer_market/transfer_market_screen.dart` | `POST /api/transfer-market/watchlist` | yes | club context | LIVE | Verified live mutation and club-context gating. |
| Place transfer bid | `frontend/lib/features/transfer_market/transfer_market_screen.dart` | `POST /api/transfer-market/listings/{listing_id}/bids` | yes | club context | LIVE | Verified live mutation and club-context gating. |
| Join GTEX competition | `frontend/lib/features/competitions/live_competitions_hub_screen.dart` | `POST /api/competitions/{id}/join` | yes | authenticated session | LIVE | Rewired active shell off `CompetitionApi.joinCompetition(...)` onto `authedApiProvider.post(...)`; authenticated joins now reject payload/session mismatch. |
| Publish GTEX competition | `frontend/lib/features/competitions/live_competitions_hub_screen.dart` | `POST /api/competitions/{id}/publish` | yes | `manage_competitions` | LIVE | Frontend button now follows permission truth; backend enforces `manage_competitions` when the active shell sends auth. |
| Launch GTEX competition | `frontend/lib/features/competitions/live_competitions_hub_screen.dart` | `POST /api/competitions/{id}/launch` | yes | `manage_competitions` | LIVE | Same fix as publish; scoped admins without the permission are blocked. |
| Join hosted competition | `frontend/lib/features/competitions/live_competitions_hub_screen.dart` | `POST /hosted-competitions/{id}/join` | yes | authenticated session | LIVE | Verified existing live hosted competition contract. |
| Launch hosted competition | `frontend/lib/features/competitions/live_competitions_hub_screen.dart` | `POST /hosted-competitions/{id}/launch` | yes | host user or admin | LIVE | Verified hosted competition launch stays on hosted engine auth rules. |
| Join streamer tournament | `frontend/lib/features/competitions/live_competitions_hub_screen.dart` | `POST /streamer-tournaments/{id}/join` | yes | authenticated session | LIVE | Verified live streamer tournament join path. |
| Publish streamer tournament | `frontend/lib/features/competitions/live_competitions_hub_screen.dart` | `POST /streamer-tournaments/{id}/publish` | yes | host/admin session per streamer engine | LIVE | Verified live streamer engine mutation path. |
| Trigger real-player import | `frontend/lib/features/profile/profile_admin_screen.dart` | `POST /internal/ingestion/real-players/import` | yes | admin + `manage_manager_catalog` | LIVE | Frontend hides the action without the permission; backend now rejects scoped admins lacking catalog permission. |
| Resume import batch | `frontend/lib/features/profile/profile_admin_screen.dart` | `POST /internal/ingestion/real-players/batches/{batchId}/resume` | yes | admin + `manage_manager_catalog` | LIVE | Same permission hardening as trigger import. |
| Issue player share market | `frontend/lib/features/profile/profile_admin_screen.dart` | `POST /players/{player_id}/shares/issue` | yes | admin + `manage_manager_supply` | LIVE | Frontend hides the action without the permission; backend now rejects scoped admins lacking supply permission. |
| Open God Mode | `frontend/lib/features/profile/profile_admin_screen.dart` / `frontend/lib/features/profile/profile_god_mode_screen.dart` | `GET /api/admin/god-mode/bootstrap` | n/a | admin + audit permission (`view_audit_log`) | LIVE | `canAccessGodMode` now depends on real audit permission, not just broad admin role. |
| Open clips feed | `frontend/lib/features/home/home_screen.dart` / router | none | n/a | authenticated session | LIVE | Guests now hit an explicit blocked route screen; the live feed mounts only for sessions that can satisfy backend auth/identity. |
| Like/share clip | `frontend/lib/features/viral_feed/presentation/viral_feed_screen.dart` | `POST /events/clip` via `EventService` and `gteReliableEventQueue` | yes | authenticated session + identity headers | LIVE | Verified active shell configures the reliable event queue in `main.dart`; guest route access is now blocked before these actions mount. |
| Join federation from world summary | `frontend/lib/features/world/world_screen.dart` | none | no | club/federation context not yet wired | BLOCKED | Kept explicitly blocked instead of faking a federation join. |
| Open native 3D | `frontend/lib/features/match/match_screen.dart` | none | no | native bridge not present | BLOCKED | Truthfully blocked. |
| Simulate match | `frontend/lib/features/match/match_screen.dart` | none | no | none | DEMO | Explicitly local/demo only. |
