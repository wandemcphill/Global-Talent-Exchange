# One ZIP Package

This single ZIP contains:
- your original V1 build bible in `v1_original/`
- the upgraded V2 package in `v2/`

Use `v2/09_CODEX_MASTER_PROMPT_V2.md` as the main launch file for Codex after reading the earlier V2 files in order.
