Desktop salvage created on 2026-04-21.

Purpose:
- Hold GTEX workspaces and loose desktop artifacts that were outside the active `GLOBAL TALENT EXCHANGE` repo.
- Keep the desktop clean while preserving side copies, corrections, and one-off notes.

Contents:
- `workspace_copies/` contains duplicate GTEX folders moved off the desktop root without merging them into the active repo.
- `notes/` contains loose GTEX text notes that did not belong in source folders.

Related runtime artifacts:
- Unity and device logs that belonged in temporary storage were moved under `tmp/`.
