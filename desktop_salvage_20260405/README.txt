Desktop salvage created on 2026-04-05.

These files were copied out of duplicate GTEX folders from the desktop before those folders were deleted.
They were not merged into the active repo automatically.

Saved items:
- `broadcast_market_fix.patch` from the desktop root.
- `from_gtex_render_broadcast_market_hotfix/` for the workflow, backend test placeholder, and one `gte_home_screen.dart` variant.
- `from_gtex_frontend_compat_fixes/` for the second `gte_home_screen.dart` variant.
- `from_gtex_stash_thread_a_premerge/` for legacy frontend files that were not present in the main repo.
