# Original Football Simulator Reference

This folder stores the clean `Football Soccer Simulator.unitypackage` as an inert reference for the controlled GTEX original visual runtime pivot.

Do not import duplicate active `FStudio.MatchEngine` code from this package into the project. The current project already contains those simulator classes. Import only missing non-code assets when the visual bridge requires them.
