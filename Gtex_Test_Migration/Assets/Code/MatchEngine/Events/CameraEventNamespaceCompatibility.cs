using FStudio.Events;

namespace FStudio.MatchEngine.Cameras
{
    // Namespace compatibility shim for CameraSystem's existing event reference.
    internal sealed class MatchCameraActiveEvent : IBaseEvent
    {
    }
}
